import ButtonBig from "@share/ButtonBig.tsx";

export default function Home() {
  return (
    <div className="flex flex-row">
      <ButtonBig iconName={"camera"} content={"로그인"} onClick={() => {}} />
    </div>
  );
}
